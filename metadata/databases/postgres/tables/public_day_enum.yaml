table:
  schema: public
  name: day_enum
is_enum: true
array_relationships:
  - name: days_rituals
    using:
      foreign_key_constraint_on:
        column: day
        table:
          schema: public
          name: days_rituals
