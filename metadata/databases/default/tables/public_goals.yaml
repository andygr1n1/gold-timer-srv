table:
  name: goals
  schema: public
array_relationships:
  - name: child_goals
    using:
      manual_configuration:
        column_mapping:
          id: parent_goal_id
        insertion_order: null
        remote_table:
          name: goals
          schema: public
