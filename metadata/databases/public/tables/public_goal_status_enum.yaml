table:
  schema: public
  name: goal_status_enum
is_enum: true
array_relationships:
  - name: goals
    using:
      foreign_key_constraint_on:
        column: status
        table:
          schema: public
          name: goals
