table:
  name: goal_status_enum
  schema: public
array_relationships:
  - name: goals
    using:
      foreign_key_constraint_on:
        column: status
        table:
          name: goals
          schema: public
