table:
  schema: public
  name: heroes
object_relationships:
  - name: notepad
    using:
      foreign_key_constraint_on:
        column: owner_id
        table:
          schema: public
          name: notepad
array_relationships:
  - name: achievements
    using:
      foreign_key_constraint_on:
        column: owner_id
        table:
          schema: public
          name: achievements
  - name: addons
    using:
      foreign_key_constraint_on:
        column: owner_id
        table:
          schema: public
          name: addons
  - name: goals
    using:
      foreign_key_constraint_on:
        column: owner_id
        table:
          schema: public
          name: goals
  - name: goals_slides
    using:
      foreign_key_constraint_on:
        column: owner_id
        table:
          schema: public
          name: goals_slides
  - name: notes
    using:
      foreign_key_constraint_on:
        column: owner_id
        table:
          schema: public
          name: notes
  - name: sprints
    using:
      foreign_key_constraint_on:
        column: owner_id
        table:
          schema: public
          name: sprints
